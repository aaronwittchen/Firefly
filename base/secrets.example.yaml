# Example secrets file - DO NOT commit real secrets!
#
# To create the secrets manually on the cluster:
#
# APP_KEY=$(openssl rand -base64 32)
# DB_PASSWORD=$(openssl rand -base64 16)
# ROOT_PASSWORD=$(openssl rand -base64 16)
#
# kubectl create namespace firefly
#
# kubectl create secret generic firefly-secret -n firefly \
#   --from-literal=APP_KEY="base64:${APP_KEY}" \
#   --from-literal=DB_PASSWORD="${DB_PASSWORD}"
#
# kubectl create secret generic mariadb-secret -n firefly \
#   --from-literal=MYSQL_ROOT_PASSWORD="${ROOT_PASSWORD}" \
#   --from-literal=MYSQL_PASSWORD="${DB_PASSWORD}" \
#   --from-literal=MYSQL_USER="firefly" \
#   --from-literal=MYSQL_DATABASE="firefly"
